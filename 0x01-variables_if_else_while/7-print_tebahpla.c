#include <stdio.h>
#include <stdlib.h>
#include <time.h>
/**
 * main - Print all letters except q and e
 *
 * Return: always 0.
 */
int main(void)
{
	char rev;

	for (rev = 'z'; rev >= 'a'; low++)
	putchar(rev);
	putchar('\n');
	return (0);
}
