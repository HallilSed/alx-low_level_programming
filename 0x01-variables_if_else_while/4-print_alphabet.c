#include <stdio.h>
#include <stdlib.h>
#include <time.h>
/**
 * main - Print all letters except q and e
 *
 * Return: always 0.
 */
int main(void)
{
	char lowerq = 'a';

	e = 'e';
	q = 'q';
	for (lowerq = 'a'; lowerq <= 'z'; lower++)
	{
	if (low != e && low != q)
	putchar(low);
	}
	putchar('\n');
	return (0);
}
