#include <stdio.h>
#include <stdlib.h>
#include <time.h>
/**
 * main - Print all letters except q and e
 *
 * Return: always 0.
 */
int main(void)
{
	char Num;

	for (Num = 0; Num < 10; Num++)
	printf("%d", Num);
	printf("\n");
	return (0);
}
